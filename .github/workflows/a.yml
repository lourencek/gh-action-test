# .github/workflows/hello-world.yml
name: Hello World A

on:
  workflow_call:

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, world A!"

      - name: Print Date and Time
        run: date
