name: Log Timestamp

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'  # Optional: runs hourly

jobs:
  log-time:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Override remote origin with PAT
        env:
          TOKEN: ${{ secrets.PAT_TOKENA }}
        run: |
          git remote set-url origin https://x-access-token:${TOKEN}@github.com/${{ github.repository }}.git

      - name: Set up Git config
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Append timestamp to log.txt
        run: |
          echo "Run at $(date -u)" >> log.txt

      - name: Commit and push changes
        run: |
          git add log.txt
          git commit -m "Logged time: $(date -u)" || echo "No changes to commit"
          git push origin HEAD:main
